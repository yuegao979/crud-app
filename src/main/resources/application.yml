spring:
  application:
    name: crud_app
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      probability: 1.0

server:
  port: 8081

database:
  hibernate:
    url: jdbc:mysql://database-1.c1iiw2usma9s.us-east-2.rds.amazonaws.com:3306/rds_employee_database
    driver: com.mysql.cj.jdbc.Driver
    username: admin
    password: 12345678
    dialect: org.hibernate.dialect.MySQL8Dialect
    showsql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always